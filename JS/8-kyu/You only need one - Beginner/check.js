nombres = [0,1,2,3,4,5,6, 'chien', 'chat', 'vache']
valeur = 0

const bitCounts = (a, x) => {
  return x === a.find(element => element === x );
}

console.log(bitCounts(nombres, valeur))
console.log(typeof 0)
